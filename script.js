const u='Sam3360';fetch(`https://api.github.com/users/${u}/repos?sort=updated`).then(r=>r.json()).then(d=>{const l=document.getElementById('list');l.innerHTML='';d.forEach(r=>{const e=document.createElement('div');e.className='repo';e.innerHTML=`<h3><a href="${r.html_url}" target="_blank">${r.name}</a></h3><p>${r.description||''}</p><small>${r.language||''} ⭐${r.stargazers_count}</small>`;l.appendChild(e);});});